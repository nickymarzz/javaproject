# Database connection URL, including host, port, database name, and connection parameters.
# For example: jdbc:mysql://localhost:3306/your_database_name?useSSL=false&serverTimezone=UTC
db.url=jdbc:mysql://localhost:3306/game_resources?useSSL=false&serverTimezone=UTC

# Username for connecting to the database.
db.user=root

# Password for the specified database user.
# IMPORTANT: In a production environment, avoid hardcoding passwords directly in property files.
# Consider using environment variables or a secure vault system.
db.password=your_password_here